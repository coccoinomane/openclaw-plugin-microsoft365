{
  "id": "microsoft365",
  "name": "Microsoft 365",
  "description": "Adds Microsoft 365 workflow guidance using the official CLI for Microsoft 365 as backend.",
  "version": "0.1.2",
  "skills": [
    "skills"
  ],
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "account": {
        "type": "string",
        "description": "Default Microsoft 365 account UPN. Example: user@example.com."
      },
      "tenantId": {
        "type": "string",
        "description": "Microsoft Entra tenant id or verified domain. Can also be provided through M365_TENANT_ID."
      },
      "clientId": {
        "type": "string",
        "description": "Microsoft Entra application/client id. Can also be provided through M365_CLIENT_ID."
      },
      "connectionName": {
        "type": "string",
        "description": "Optional CLI for Microsoft 365 connection name. Defaults to openclaw-microsoft365."
      }
    }
  },
  "uiHints": {
    "tenantId": {
      "label": "Tenant ID or domain",
      "placeholder": "contoso.onmicrosoft.com or tenant GUID"
    },
    "clientId": {
      "label": "Client ID",
      "placeholder": "Application/client GUID"
    },
    "account": {
      "label": "Default account",
      "placeholder": "user@example.com"
    },
    "connectionName": {
      "label": "CLI connection name",
      "placeholder": "openclaw-microsoft365"
    }
  },
  "setup": {
    "providers": [
      {
        "id": "microsoft365",
        "envVars": [
          "M365_TENANT_ID",
          "M365_CLIENT_ID",
          "M365_ACCOUNT",
          "M365_CONNECTION_NAME"
        ]
      }
    ]
  }
}
